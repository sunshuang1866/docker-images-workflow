# 修复摘要

## 修复的问题
CI 基础设施问题（infra-error），CI 预检脚本日志被截断，无法定位具体错误原因，无需代码修改。

## 修改的文件
无

## 修复逻辑
CI 分析报告将此失败归类为 `infra-error`（置信度：低）。日志中 Shell 步骤 `/tmp/jenkins13668292807163518311.sh` 在执行"清理缓存"后被截断，原始错误信息不可见。对该 PR 涉及的 4 个文件进行逐一核查：

1. **Dockerfile**：与现有 5.0.1/5.0.0/4.3.0 版本的 Dockerfile 结构和模式完全一致，均无 copyright/SPDX 头，变量命名与 5.0.1 版本一致（使用 `JDKARCH`），无语法错误。
2. **meta.yml**：新增的 `5.0.2-oe2403sp3` 条目语法正确，格式与现有条目一致。
3. **doc/image-info.yml**：表格条目格式与现有条目一致。
4. **README.md**：表格条目格式与现有条目一致。

所有文件均遵循项目现有约定，不存在可识别的代码缺陷。根因在 CI 基础设施侧（预检脚本截断），非代码层面问题。强行修改代码无法修复此失败。

## 后续建议
- 获取 CI 预检脚本 `/tmp/jenkins13668292807163518311.sh` 的完整输出，定位预检失败原因
- 确认 CI 预检脚本是否对新增 Dockerfile 有额外的 license/格式检查要求

## 潜在风险
无（未修改代码）