# 修复摘要

## 修复的问题
无需代码修改。CI 失败为基础设施问题（infra-error）：CI 流水线将纯文档 PR（仅修改 `README.md`）错误路由至应用商店发布规范校验 job，导致校验工具将根目录 `README.md` 误判为不符合应用商店镜像条目路径规范。

## 修改的文件
无。PR 的代码变更（`README.md` 中更新镜像 Tags 列表）本身正确无误，无需修改。

## 修复逻辑
分析报告明确指出：
- 失败位置在 `eulerpublisher/update/container/app/update.py:273`，属于 CI 应用商店发布规范校验工具
- PR 仅修改了 `README.md` 和 `README.en.md` 的受支持镜像 Tags 列表，变更内容正确
- 根因是 CI 流水线将文档类 PR 错误地路由至应用商店发布规范校验 job
- 修复方向应在 CI 流水线配置层面（为应用商店校验 job 添加文件类型过滤，排除根目录纯文档文件），而非代码层面

根据流程规范，infra-error 类型不应当通过修改源码来解决，因此本次不实施任何代码修改。

## 潜在风险
无。不涉及代码变更。